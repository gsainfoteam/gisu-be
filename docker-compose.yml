version: '3.8'
services:
  db:
    image: postgres:15
    container_name: gisu-postgres
    restart: always
    ports:
      - '5432:5432'
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: mysecretpassword
      POSTGRES_DB: gisu_db